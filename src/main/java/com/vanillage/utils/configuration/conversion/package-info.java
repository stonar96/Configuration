/**
 * Configuration classes, that are mainly representing short-lived structure
 * objects, which are used while converting (parsing and transforming)
 * configurations. These objects are mainly used as parameters or return values
 * for the communication between parsers and deserializers or transformers and
 * serializers respectively.
 */
package com.vanillage.utils.configuration.conversion;
