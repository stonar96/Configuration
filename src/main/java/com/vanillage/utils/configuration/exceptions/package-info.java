/**
 * Exceptions which are thrown by various configuration operations such as
 * parsing, transforming and loading. These exceptions are mainly wrapper for
 * the underlying cause exceptions, which depend on the implementation, to
 * define the thrown exception data types in an abstract context.
 */
package com.vanillage.utils.configuration.exceptions;
